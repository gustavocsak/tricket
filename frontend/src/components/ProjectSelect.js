import React from 'react';
import Form from 'react-bootstrap/Form';

const ProjectSelect = () => {
    return (
        <>
            <Form.Select>
                <option>Select your project</option>
                {/* create state for projects from database */}
            </Form.Select>
        </>
    );
};

export default ProjectSelect;
